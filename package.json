{
  "name": "kalshi-btc-predictor",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "build:wasm": "cd packages/sim-wasm && pnpm build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^3.1.0",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.6.0",
  "engines": {
    "node": ">=18"
  }
}
